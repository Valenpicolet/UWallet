using Cripto.Entidades;
using Cripto.Servicios;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Cripto.Repositorio
{
    public class CotizacionesRepositorio
    {
    //    public List<Cotizaciones> GetCotizaciones()
    //    {
    //        var cotizaciones = new List<Cotizaciones>();
    //        var sql = $"";
    //        var tablaResultado = DBHelper.GetDBHelper().ConsultaSQL(sql);
            
    //        foreach (DataRow fila in tablaResultado.Rows)
    //        {
    //            var cotizacionFila = new Cotizaciones();
    //            cotizacionFila.Fecha_cotizacion = fila["fecha_cotizacion"];
    //            cotizacionFila.Hora_cotizacion = fila["hora_cotizacion"];
    //            cotizacionFila.Precio = Convert.ToInt64(fila["precio"].ToString());

    //            Monedas
    //            var cripto = new Criptomonedas();

    //        }
    //        return cotizaciones;
    //    }
    }
}
